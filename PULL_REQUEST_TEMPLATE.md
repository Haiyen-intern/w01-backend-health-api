## Ý nghĩa của PR 
- Khởi tạo môi trường Backend đơn giản bằng thư viện `http.server`.
- Tạo API endpoint `/api/health` trả về kết quả JSON trạng thái hệ thống.

## Kết quả kiểm thử dưới Local
- [x] Đã khởi chạy server thành công tại cổng 8000.
- [x] Test qua Postman trả về mã 200 OK và JSON đúng cấu trúc.